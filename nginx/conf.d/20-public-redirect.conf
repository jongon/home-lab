server {
  listen 80;
  server_name ha.example.com jellyfin.example.com navidrome.example.com jellyseerr.example.com;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}